# 【圣旨】edict-003 · AI 情报引擎 · 阶段 A：纵切跑通

> 颁发日期：2026-03-10
> 前任圣旨：edict-002-finance-reshaping（仍有效，本圣旨为其子任务）

## 旨意
建设设置页面 + 跑通第一条 AI 链路（Groq 新闻摘要），让皇上亲眼看到真实 AI 输出。

## 背景
- 前端 13 面板可展示交互（mock 数据）
- 后端 22 个 service 已写好但未运行验证
- 0 个 API Key 已配置，AI 引擎从未实际调用过
- 皇上确定最终方向：多模型可插拔 AI 调度层 + 情报分析师级产出
- 执行节奏：A（纵切跑通）→ B（统一接口）→ C（多模型路由）→ D（情报产出）

## 本阶段范围（两个任务并行）

### 任务 1：设置页面

TitleBar [⚙️ 按钮] → 打开设置页面

```
设置页面结构：
├── 数据源管理
│   ├── 现有数据源列表（55+ RSS + 8 API 源）
│   ├── 每个源：名称 / 类型 / 状态灯 / 最后成功时间 / 错误信息
│   ├── + 添加 RSS 源（URL + 名称 + 分类）
│   └── 删除 / 启用 / 禁用
│
├── AI 模型管理（先做 Groq + Ollama 两个）
│   ├── 已配置模型列表 + 状态灯 + 测试连接按钮
│   └── + 添加模型（选类型 → 填 Key → 测试 → 保存）
│
└── API Key 配置
    ├── FRED / EIA / WTO（数据源）
    └── Groq / Claude（AI 模型，后续扩展 OpenAI/Gemini/DeepSeek）
```

### 任务 2：第一条 AI 链路

```
RSS 采集（已有代码）
  → 新闻入 SQLite（已有代码）
  → Groq 批量摘要（已有代码，需配 Key 跑通）
  → 摘要结果写回 DB（已有代码）
  → News Feed 面板展示 AI 摘要 + 情感标签（需前端增强）
  → 皇上看到真实 AI 输出
```

## 底线（不可违逆）

### 禁止
- ❌ 禁止硬编码 API Key 到源码中（必须走 tauri-plugin-store）
- ❌ 禁止未经实际运行就报告"已完成"
- ❌ 禁止为了赶进度跳过设置页面直接在代码里配 Key
- ❌ 禁止锁死模型名称——即使先做 Groq，接口也要为后续多模型留口子

### 必须
- ✅ 设置页面必须能保存 Key 并立即生效（不重启应用）
- ✅ 测试连接按钮必须真实发请求并返回结果
- ✅ 状态灯必须反映真实连接状态（不是假数据）
- ✅ 至少一条 RSS 新闻经过 Groq 摘要后在前端展示

## 验收标准

| # | 标准 | 验证方法 |
|---|------|----------|
| 1 | 设置页面可从 TitleBar 打开 | 点击按钮，页面出现 |
| 2 | 能在设置里输入 Groq API Key 并保存 | 输入 → 保存 → 重启应用 → Key 仍在 |
| 3 | 测试连接显示 Groq 在线 | 点击测试 → 状态灯变绿 |
| 4 | 数据源列表显示 55+ RSS 源状态 | 打开设置 → 看到列表 + 状态灯 |
| 5 | News Feed 中至少 1 条新闻有 AI 摘要 | 打开主界面 → 看到新闻带摘要标签 |

## 约束
- 作用范围：`src/`（新增设置页面组件）+ `src-tauri/`（新增设置相关 command）
- 不动现有面板逻辑，只增强 News Feed 的展示
- AI 接口设计预留 `AiProvider` trait 的雏形，但不做完整多模型路由

## 确定性目标
- **最低目标**：设置页面能配 Key + 一条 AI 摘要在前端展示
- **最高目标**：设置页面完整 + 数据源增删 + 多个 RSS 源跑通摘要 + 状态灯全亮

## 验真令
所有"已完成"声明必须附带实际执行的命令和输出。禁止"应该可以"、"理论上"等模糊表述。

## 止损令
同一问题修改超过 3 次未果，必须暂停并报告。

## 未来方向（本阶段不做，仅记录）
- 阶段 B：统一 AiProvider trait，多模型可插拔
- 阶段 C：多模型交叉验证 + 智能路由
- 阶段 D：数据源 AI 审计 + 实时情报流 + 研报生成
- 最终目标：情报分析师级 AI 引擎，支持 N 个 AI 模型提供商
